module assets

import { Price } from "./state"

struct AssetCounter {
    asset_class: AssetClass
    count: Int
    timestamp: Time // timestamp of price update
    price: Price
}

struct AssetsGroup{
    assets: []AssetCounter
}