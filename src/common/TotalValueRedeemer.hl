module common_TotalValueRedeemer

import { PriceFeed } from "./PriceFeed"

enum TotalValueRedeemer {
    Reset
    Sum {
        price_feeds: []PriceFeed // sorted in the order that correctly updates the running_hash
    }
    Upgrade
}